# Base URL the app's axios instance (services/mutator/custom-instance.ts) sends requests to.
NEXT_PUBLIC_API_BASE_URL=

# OpenAPI spec URL used by `npm run orval` to generate services/generated.
API_DOCS_URL=
